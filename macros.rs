//macros are called at compile time.
//they are more powerful that function
//types declarative macros.
//macros are moddules that accepts code and write codes.

fn main() {
    let v1: Vec<u32> = vec![1, 2, 3, 4];
    let v2: Vec<&str> = vec!["a", "b", "c", "d"];
}
